# Copy to /etc/nami-collector.env on the server. Never commit real values.
NAMI_USER=
NAMI_SECRET=
NAMI_HOST=https://open.sportnanoapi.com
OUT_DIR=/var/lib/nami-collector
# Optional. If Nami fails, collector still exits 0 using FotMob scores only (no lottery SP).
ALLOW_FOTMOB_FALLBACK=1
